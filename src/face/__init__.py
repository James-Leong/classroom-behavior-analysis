"""Face recognition building blocks (detector/gallery/matcher).

This package intentionally keeps APIs lightweight so the top-level `FaceRecognizer`
can remain backward-compatible while improving robustness.
"""
