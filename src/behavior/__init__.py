"""Behavior analysis package.

This package provides:
- Video action model inference (Torchvision-based by default)
- Mapping model categories to classroom behavior labels
- Aggregation into per-student (by name) behavior statistics

All components are optional and are only used when enabled via CLI.
"""

from __future__ import annotations
